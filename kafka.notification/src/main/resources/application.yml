server:
  port: 8183
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms3_notification
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  kafka:
    consumer:
      auto-offset-reset: earliest
